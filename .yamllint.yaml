---
extends: default

ignore:
  - config/crd/bases/
  - config/rbac/
  - bin/

rules:
  line-length:
    max: 120
  truthy:
    allowed-values: ["true", "false"]
  comments:
    min-spaces-from-content: 1
  document-start: disable
